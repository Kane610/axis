from axis import AxisDevice

config = {'alias': 'alias',
          'name': 'name',
          'host': '10.0.1.20',
          'username': 'root',
          'password': 'pass',
          'location': 'location'}

device = AxisDevice(config)
