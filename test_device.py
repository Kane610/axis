from axis import AxisDevice

config = {'alias': 'alias',
          'name': 'name',
          'host': '10.0.1.65',
          'username': 'root',
          'password': 'passs',
          'location': 'location'}

device = AxisDevice(config)
