[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    # TYPE_CHECKING and @abstractmethod blocks are never executed during pytest run
    if TYPE_CHECKING:
    @abstractmethod
