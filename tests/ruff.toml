# This extend our general Ruff rules specifically for tests
extend = "../pyproject.toml"

[lint.isort]
known-first-party = [
    "axis",
    "tests",
]
known-third-party = [
    "httpx",
    "pytest",
    "respx",
]
forced-separate = [
    "tests",
]
